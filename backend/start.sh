# Start script for the FastAPI backend with improved code

# Activate environment if needed
# source venv/bin/activate

# Start the FastAPI server
uvicorn main:app --reload --host 0.0.0.0 --port 8000
